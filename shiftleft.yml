source:
  scan: previous
  #  branch: main
build_rules:
  - id: "Allow zero high SAST vulnerabilities"
    finding_types:
      - vuln
      - secret
      - oss_vuln
    cvss_31_severity_ratings:
      #- high
      #- critical
      #threshold: 0
    options:
      reachable: true
      num_findings: 10
